#include <stdio.h>
int main()
{
    float r, pi = 3.14, c;
    printf("give me radius of circle :") + scanf("%f", &r);
    c = 2 * pi * r;
    printf("this is circumference of circle  %f\n", c);
    return 0;
}